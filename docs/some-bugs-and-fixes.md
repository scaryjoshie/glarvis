I think one of the problems that I have right now is that it seems like the original, like, speech to text stream a lot of modifications put into it, like, you know, actions, stuff like that that are injected into it. And it feels like we're using that exact same text stream for the transcriber when it really isn't supposed to be the case. And then now we have to do workarounds. To fix that. I feel like it would be better if the text stream was, you know, raw, and then if it's being passed to the orchestrator. Then we do all those operations and injections. But it's totally fine for, like, a any tool I guess, you know, it'd be fine for a tool to see the the injected version, but I think a tool should be exposed to the raw version of a of a speech to text as well if it isn't already. That would be optimal. I wanna clean those information pipelines up because I feel like there are lot of cases where you know, something was written a certain way, and then you have to we kind of hijack it to make it fit the next tool. But know, the the cleanest solution is to build those things out to be extremely modular and extremely repurposable. In the first place. Then kind of work from there.

Yeah. I guess I would like to hear your opinion on all of those things, I I feel like there's I feel like, you know, if I want this product to be open sourceable, I'm I'm planning on open sourcing it. All these pipelines have to be super clean. There have to be like, it has to be really easy for anyone to make a tool You know, right now, this only works for Windows, but I want it to work for Mac as well. So making tools and and contributing has to be really simple and really easy, and then I feel like the core system has to be really well thought out. I mean, like, you know, I've talked to several LLMs, and and they've said the code is clean. But I think the code doesn't have to just be clean. It has to be, like, really clean. Really organized, and the general architecture has to be very strong as well. I think it's really important that we go over that. And make sure it's good. 

Think in the meantime, it makes sense to talk about tools tools that that I want So the I think the end goal is for every single tool to be configurable in settings. So what the system will do is it will scan the tools folder think we should separate the tools folder into, like, default tools. Then into tools that the LLM can create or, like, other tools. Right? So, like, I'm gonna have a tool that opens Minecraft, but before it does, it closes everything else. Like, that's gonna be a fun tool to have. But obviously, that's, like, not a great tool to, like, force onto everybody, so it'll go into, like, the specialized tools area. Right? So I would really like a system that basically scans for all the tools, that are defined. So, basically, I guess, every object that, like, is a tool. Inherits from the base tool. And then I guess checks it. And imagine every single tool will have its own, like, settings config file or something like that. You can configure all these things and work and store all these things. And then you can click on the tools And and and some tools, right, like, are only accessible when you're in other tools. Like, they're, like, nested tools or something along those lines. So it would show that, like, tree sort of like a file structure. I think it sort sort show it in, like, a file structure type way. And then clicking into any tool would give you, like, a settings menu on the right, which shows you all the settings that are exposed via config. And lets you change them. I think that would be, like, the optimal endgame.

I would really love for the transcriber to snap to the bottom if possible. We can turn off, like, snapping or not snapping in the settings menu. For the transcriber. But right now, it'd be cool if it snaps wonder if you could even, like, if it snaps to the side, then instead of stacking, like that's kind of weird actually because the text flows to the side. But it could, like, stack vertically instead. But right now, just snapping to the bottom is is all we need.

The beginning, the listening prompt, like the, oh, I'm listening thing. Is kind of cut off. It's cut off by the fade out on the left. Maybe I'm thinking, like, it starts out in an okay spot where it's not fading out, then it's simply just, like, as you type more and more it'll get forced to the left. Doesn't have to be complicated. I imagine it being really simple. So if the whatever imposition you're thinking is complicated, it's going to be simple. Probably.

All tool calls should 

Should tools be able to give their own system state type reports? Maybe we do a nested print, with:
[GLOBAL]
- Task A: status
- Task B: status
    - Task C: status

To be honest, I don't really know what's best practice, so let me knowI'm testing again. T

"Minerva overlay is already active for voice commands", even when the transcriber is closed. So two problems:
1. Minerva overlays count as windows, which is bad for multiple reasons
2. Closed transcription windows for some reason linger in active state, even though I can't see them on debug.